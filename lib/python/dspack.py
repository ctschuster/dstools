import sys, os, re
import subprocess



def single_pack(loc):
    return "--not-yet-implemented--"


def execute_pack(loc):
    if (len(modeargs) != 0):
        for loc in modeargs:
            single_pack(loc)
    else:
        print("no targets specified")
    return
